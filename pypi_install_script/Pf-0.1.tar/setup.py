from distutils.core import setup
setup(
  name = 'Pf',
  packages = ['Pf'], # this must be the same as the name above
  version = '0.1',
  description = 'A dead simple task runner library with dependency injection and multi threading',
  author = 'Xinyu Zhang',
  author_email = 'xiz368@eng.ucsd.edu',
  url = 'https://github.com/BenBBear/Pf', # use the URL to the github repo
  download_url = 'https://github.com/BenBBear/Pf/tarball/0.1', # I'll explain this in a second
  keywords = ['multi threading', 'task-runner', 'dependency injection'], # arbitrary keywords
  classifiers = [],
)