from setuptools import setup

setup(name='BytesZip',
      version='1.0.1',
      description='Packing data in bytes of string',
      packages=['BytesZip'],
      author_email='jklenot123@gmail.com',
      zip_safe=False)
