from setuptools import setup

setup(
    name='django-saved',
    version='2020.4.8',
    install_requires=[
        'Django',
        'setuptools',
    ],
    packages=[
        'django_saved',
    ],
)
