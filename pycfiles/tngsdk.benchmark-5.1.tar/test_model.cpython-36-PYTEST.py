# uncompyle6 version 3.7.4
# Python bytecode 3.6 (3379)
# Decompiled from: Python 3.6.9 (default, Apr 18 2020, 01:56:04) 
# [GCC 8.4.0]
# Embedded file name: /Users/manuel/tango/tng-sdk-benchmark/models/test_model.py
# Compiled at: 2019-03-05 11:04:32
# Size of source mod 2**32: 2458 bytes
import builtins as @py_builtins, _pytest.assertion.rewrite as @pytest_ar, unittest, ietf_ped_model as generated_model, yaml, os, pyangbind.lib.pybindJSON as pybindJSON
MODEL_NAME = 'ietf_ped_catalog'
EXAMPLE_DIR = 'examples/'

def check_against_model(yaml_path):
    """
    Open a given YAML file and tries to load it into
    model that was generated by pyangbind.
    Fails if the YAML file is not compatible to the
    model.
    """
    with open(yaml_path, 'r') as (f):
        ped_data = yaml.load(f)
        ped_model = pybindJSON.loads_ietf(ped_data, generated_model, MODEL_NAME)
        return ped_model


class TestStringMethods(unittest.TestCase):

    def test_example_peds_against_model(self):
        example_peds = os.listdir(EXAMPLE_DIR)
        print('Validating: {} PED files.'.format(len(example_peds)))
        for ep in example_peds:
            self.assertIsNotNone(check_against_model(os.path.join(EXAMPLE_DIR, ep)))
            print("File: '{}' is valid against model '{}'.".format(ep, MODEL_NAME))